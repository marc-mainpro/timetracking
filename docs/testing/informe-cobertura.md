# Informe de cobertura (placeholder)

Aún no existe código de dominio ni de aplicación (iteración 1: solo
scaffolding). Este documento se completará a partir de la iteración 2, con el
resumen del informe JaCoCo generado por `mvn verify`:

- Cobertura de línea/rama de `domain` (objetivo: ≥ 90 %).
- Cobertura de línea/rama de `application` (objetivo: ≥ 80 %).
- Enlace al reporte HTML generado en `backend/target/site/jacoco/index.html`.

Cada tarea que añada lógica de negocio debe actualizar este documento con las
cifras vigentes tras `mvn verify`.
