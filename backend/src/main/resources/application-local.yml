spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/timetracking}
    username: ${DB_USER:timetracking}
    password: ${DB_PASSWORD:timetracking}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
